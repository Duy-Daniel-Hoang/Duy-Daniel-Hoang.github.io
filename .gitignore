node_modules
dist
.DS_Store

# Internal admin mockup — dev-only, never publish source or its exclusive assets
src/mockups/
public/assets/flickrz/scene-cut*.webp
public/assets/flickrz/jiho-sketch*.webp
# Only the disposable mockup source drawing stays out — the case-study demo
# videos in this same folder are real production assets and must be tracked.
public/assets/makinarocks/original-drawing*.jpg
